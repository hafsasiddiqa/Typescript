"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPaths = void 0;
const getParsedGitignores_1 = require("./getParsedGitignores");
const startsWithBang = (str) => str.startsWith('!');
const removeStartBang = (str) => str.replace(/^!/, '');
const getPaths = (params) => {
    const parsedPaths = (0, getParsedGitignores_1.getParsedGitignores)(params);
    const incExc = parsedPaths.reduce((acc, line) => {
        if (startsWithBang(line)) {
            acc.include.push(removeStartBang(line));
        }
        else {
            acc.exclude.push(line);
        }
        return acc;
    }, {
        include: [...params.include],
        exclude: [...params.exclude],
    });
    return incExc;
};
exports.getPaths = getPaths;
